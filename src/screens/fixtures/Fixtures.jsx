import React from 'react';
import { Center, Text } from 'native-base';

const Fixtures = (props) => {
    const [plTeams, setPlTeams] = React.useState([]);

    return (
        <Center>
            <Text>Fixtures</Text>
        </Center>
    );
};

export default Fixtures;
